<option>
  {{#if inherit}}
    Inherit ({{inherit.name}})
  {{/if}}
</option>

{{#each alertSchedulers as |alertScheduler|}}
  <option value={{alertScheduler.id}} selected={{eq alertScheduler.id selected}}>{{alertScheduler.name}}</option>
{{/each}}
