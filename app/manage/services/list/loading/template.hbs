{{#sy-list-loading as |section|}}
  {{#if (equal section 'actions')}}
    {{#link-to 'manage.services.new' class='btn btn-success'}}
      <i class="fa fa-plus"></i>&nbsp;New service
    {{/link-to}}
  {{else if (equal section 'header')}}
    <th>Name</th>
    <th class="visible-lg">Description</th>
    <th>Command</th>
  {{/if}}
{{/sy-list-loading}}
